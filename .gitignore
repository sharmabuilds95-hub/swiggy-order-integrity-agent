# secrets / tokens — never commit these
.env
*.token
token.json
tokens/
.mcp-auth/

# real personal order history — private, used to derive anonymized test
# fixtures; the raw copy must never hit the public repo (PII: what/when/where
# the user ordered). Committed fixtures live under tests/fixtures/ anonymized.
tests/fixtures/real/

# live-run working state — holds REAL order ids, address id, GPS coordinates
# and UPI transaction data from actual placed orders. Never commit (PII).
scratchpad/
server_log.txt

# python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# node (mcp-remote's own cache, if it lands locally)
node_modules/

# os
.DS_Store
Thumbs.db
